# AGENTS.md — GRAPHIA bench (V)

**Name:** GRAPHIA (Γραφία — the writing; Greek goddess of writing). Chosen for the
finding this bench exists on: **concepts live in the WRITE direction of ΔW**
(chora exp10, 2026-09-18 — write-orthogonal, read-shared, mixture-interpolating).

**Scope:** the experiment notes, the paper, and the follow-up runs of the Exp10 line
(orthogonality & composable adapters). Exp11 (horizontal matrix) is registered on the
chora board and reports its rows HERE, where the geometry lives.

**Law inherited:** the CHORA workspace law (share inputs never histories; nothing crosses
without a hash; single writer; pre-register; negative results first-class) applies to this
bench. Bytes consumed from chora are cited `(path, sha256)` — never copied blind.

**Site:** repo root `index.html` (GitHub Pages, light theme per the owner's ruling:
tables via great_tables; the dark glass belongs to the parks' own fronts, this is a study).
**Paper:** `paper/exp10-paper.{tex,pdf}` — compiled by tectonic (LETHE's hands).
**Notes:** `notes/`.

Written 2026-09-18 by lola@LETHE at the owner's "单独建一个Bench".
